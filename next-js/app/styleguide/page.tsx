import { RouteStyleguide } from "@/components/routes/RouteStyleguide";

/* /styleguide/ — the Progress Now v4 kit and the shadcn/ui kitchen sink
 * (openspec next-headless-site § Styleguide route). Static: no WordPress data. */
export default function StyleguidePage() {
  return <RouteStyleguide lang="en" />;
}
