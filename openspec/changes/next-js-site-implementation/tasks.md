## 1. Rename `site/` → `nuxt-js/` (shippable alone)

- [x] 1.1 `git mv site nuxt-js`; fix self-references (`nuxt-js/README.md` title, `test/unit/shared-source-drift.test.ts` comments/describe); in `nuxt-js/`: `npm ci && npm run lint && npm run typecheck && npm test && npm run generate:mock && npm run verify:output` — all green (relative `../wp-content` paths keep the same depth)
- [x] 1.2 Update every path reference: root `README.md` (8), `docs/deployment.md` (11), `wp-content/themes/progressnow/bin/worktree-bootstrap.sh`, theme `README.md` (3), `inc/payloads.php` comment, `openspec/changes/nuxt4-static-platform/{proposal,design,tasks}.md` + its `specs/**` (`site/` → `nuxt-js/`; the future `.github/workflows/{ci,rebuild-site}.yml` from nuxt4 tasks 6.x/7.3 must use `nuxt-js/` — no workflow files exist yet)
- [x] 1.3 Rewrite root `README.md` around "one CMS, three frontends": PHP theme (server-rendered, no Node) / `nuxt-js` (static rendition + PHP shell handoff) / `next-js` (headless, Node-hosted); per-install decision table (frontend, `CHAPTER_REBUILD_TRANSPORT`, `CHAPTER_CANONICAL_ORIGIN`, `CHAPTER_STATIC_DIR`); links to each app's README
- [x] 1.4 Verify: `grep -rn "site/"` outside `components/site` and `openspec/changes/archive` returns nothing actionable; commit "Rename site/ to nuxt-js/" (one mechanical commit, revertable)

## 2. Scaffold `next-js/` and the test harness (no UI yet)

- [x] 2.1 `create-next-app` → `next-js/` (Next 16 pinned major, App Router, TS strict, `app/` + `lib/` + `components/` + `test/`, Node 22 engines); `next.config.ts`: `output: 'standalone'`, `trailingSlash: true`, `poweredByHeader: false`, `cacheComponents: true`, `experimental.viewTransition`; `.env.example` documenting `WP_API_BASE`, `WP_ORIGIN`, `NEXT_PUBLIC_SITE_ORIGIN`, `CHAPTER_REBUILD_SECRET`, `WP_BUILD_STATUS_URL`, `IMAGE_HOSTS`, `MOCK_API`
- [x] 2.2 Test first `test/unit/shared-source-drift.test.ts` (schemas.ts, tailwind.css with the Nuxt normalization, categories.json); then copy theme `src/lib/schemas.ts` → `lib/schemas.ts`, `src/css/tailwind.css` → `app/globals.css`, `categories.json`; Tailwind v4 via `@tailwindcss/postcss` (no `tailwind.config.*`), `tw-animate-css`
- [x] 2.3 shadcn/ui init: `components.json` (new-york, neutral, cssVariables, lucide-react, aliases `@/components`, `@/lib/utils`); `lib/utils.ts` (`cn`); install `button` as smoke and assert its background resolves to `--primary` (component test)
- [x] 2.4 Lint/format: ESLint flat config (next, typescript-eslint, `eslint-plugin-jsx-a11y` strict), tokens-only rule (fail on `[#hex]` / arbitrary radius in class strings), Prettier; `lint`, `typecheck` scripts
- [x] 2.5 Vitest: `node` project (lib) + `jsdom` project (components) with RTL, jest-dom, user-event, jest-axe; `test:unit` script; smoke tests for both projects
- [x] 2.6 Contract fixture tests: port `nuxt-js/test/unit/contracts.spec.ts` → `test/unit/contracts.spec.ts` reading `../wp-content/themes/progressnow/tests/fixtures/*.json` through `lib/schemas.ts`
- [x] 2.7 Mock API: port `nuxt-js/shared/mock-api.ts` → `test/mock/api.ts` (framework-free, same overlays, `MOCK_ORIGIN`, `MOCK_CONTENT_VERSION`) + `test/mock/server.mjs` (node `http`, port 8787, request log at `GET /__mock/requests`, mutation hooks `POST /__mock/posts/{slug}` and `POST /__mock/fail` for e2e); unit test: mock route list equals the `routes-manifest.json` fixture; `dev:mock` script runs mock + `next dev`
- [x] 2.8 Playwright: `playwright.config.ts` with projects `e2e` and `a11y` sharing one `webServer` (mock + `next build && next start` with `WP_API_BASE=http://127.0.0.1:8787/wp-json/progressnow/v1`); `@axe-core/playwright` with `axe-core` pinned; `test/e2e/a11y/scan.ts` helper (rules `wcag2a,wcag2aa,wcag21aa,wcag22aa` errors, `best-practice` warnings, per-page JSON report to `test-results/axe/`); smoke: `/` is 200
- [x] 2.9 CI: create `.github/workflows/ci.yml` (coordinate with nuxt4 task 7.3) with a `next-js` job on Node 22: `lint`, `typecheck`, `test:unit`, `build`, `test:e2e`, `test:a11y`; upload `test-results/axe/**` and styleguide screenshots; no WordPress service
- [x] 2.10 Bundle budget: `scripts/check-bundle-budget.mjs` reading the build manifest for the front route's first-load JS vs `budget.json` (130 kB gzipped); wired after `build` in CI; unit test for the parser

## 3. Data layer, routing, revalidation receiver

- [x] 3.1 Test first: port `nuxt-js/test/unit/routes.spec.ts` → `test/unit/routes.spec.ts` unchanged; implement `lib/routes.ts` as a verbatim port of `nuxt-js/app/lib/chapter/routes.ts` (normalizePath, langForPath, resolveRoute)
- [x] 3.2 Test first `test/unit/env.spec.ts` (missing/malformed vars fail fast; `MOCK_API=1` relaxes the secret) and `test/unit/api.spec.ts` (dev throw / prod `ApiError('progressnow_contract')`, 404 → `ApiError` 404, network error); implement `lib/env.ts` (zod) and `lib/api.ts` (`import 'server-only'`, one function per endpoint, `validate`)
- [x] 3.3 Cache layer `lib/data/{site,routes,front,page,posts,post,events,event}.ts`: `'use cache'` + `cacheTag('content', 'routes'|'site'|<key>)` + long `cacheLife`; per-query short `cacheLife` for search; unit tests for tag naming (`post:{lang}:{slug}` …); document the data-cache-only fallback toggle in `lib/data/README.md`
- [x] 3.4 Test first `test/unit/links.spec.ts` (WP-origin → relative; `/wp-admin|wp-login.php|wp-json|wp-content|feed` and file URLs stay absolute; external unchanged + `rel=noopener`); implement `lib/links.ts` (`toAppHref`) and `components/site/SiteLink.tsx` (wraps `next/link`)
- [x] 3.5 `app/layout.tsx` (manifest → `<html lang>`, `/site?lang=` slot, nonce from headers), `app/[[...slug]]/page.tsx` catch-all (`generateStaticParams` from `/routes` both languages, `dynamicParams = true`, resolver → `components/routes/Route*.tsx` placeholders rendering kind + title), `not-found.tsx`, `error.tsx` placeholders; e2e: every mock route renders in `en`/`es`, `/about` → 308 `/about/`, unknown path 404 with zero mock requests (via `/__mock/requests`)
- [x] 3.6 Receiver test first `test/unit/signing.spec.ts` + `test/unit/rebuild-receiver.spec.ts`: PHP vectors (generate once with `php -r "echo hash_hmac('sha256', ts.'.'.body, secret);"`, commit `test/fixtures/signing-vectors.json`), ±300 s window, 9–11 digit timestamp, constant-time compare, replay → 401, >16 KB → 413, bad shape → 400, success → 202 `{ buildId, status: "started" }` and tags invalidated; implement `lib/signing.ts`, `lib/replay-cache.ts` (LRU), `app/api/rebuild/route.ts`, `lib/build-status.ts` (signed callback, 3 attempts, backoff, logged failures)
- [x] 3.7 `app/api/health/route.ts` (`{ ok, buildId }` from `NEXT_PUBLIC_BUILD_ID` = git sha at build; no upstream) and `app/api/events/route.ts` (`lang`, `from`, `to` → cached `/events`); unit tests
- [x] 3.8 e2e `test/e2e/receiver.spec.ts`: mutate a post title via `/__mock/posts/{slug}`, POST a correctly signed webhook to `/api/rebuild`, reload → new title; stale timestamp → 401 and title unchanged
- [x] 3.9 Structured logging `lib/log.ts` (route, status, duration, `requestId`; receiver outcomes); unit test that secret/signature never appear in log lines

## 4. Design system and accessibility foundation

- [x] 4.1 Audit `nuxt-js/app/components/site/**` imports of `@/components/ui/*`; install exactly that shadcn/ui set (expected: accordion, alert-dialog, aspect-ratio, badge, breadcrumb, button, calendar, card, carousel, collapsible, command, dialog, popover, separator, sheet, tooltip); component test + jest-axe per installed component; unused components absent (test)
- [x] 4.2 Test first `test/unit/a11y-settings.spec.ts` (defaults, `chapter-a11y` shape, `rgv-dsa-a11y` one-time migration, 16/18/20 px, attribute mapping) and `test/unit/a11y-bootstrap.spec.ts` (generated inline script applies `data-text-size|data-contrast|data-motion` from storage before paint); implement `lib/a11y-settings.ts`, `components/a11y/A11yProvider.tsx`, `lib/a11y-bootstrap.ts` (nonce'd inline script string)
- [x] 4.3 Motion: CSS + `useReducedMotion()` (media query OR widget) gating `tw-animate-css` utilities and view transitions; `components/nav/RouteTransition.tsx` (`<ViewTransition>` wrapper, instant swap when reduced or `startViewTransition` missing); unit tests for the gate
- [x] 4.4 Primitives and small site components with RTL + jest-axe tests first: `ArrowGlyph`, `StarGlyph`, `DuotoneImage` (next/image), `DashedNote`, `CtaCard`, `LinkListCard`, `WordmarkLockup`, `PageHeader`, `SubscribeStrip`, `EmailSubscribeStrip`, `CategoryTag`, `ImageSlot`, `FaqAccordion`, `BlockA11yNote`, `BlockGoodToKnow`, `BlockAgenda`, `BlockMap` — tone bands via `data-tone`
- [x] 4.5 Fonts/assets same-origin: `rewrites()` for `/wp-content/themes/progressnow/static/:path*` → `WP_ORIGIN` with `Cache-Control: public, max-age=31536000, immutable` (headers config); preload links for Bowlby One + Public Sans; e2e asserts all font requests same-origin, no CORS console errors, no third-party font hosts
- [x] 4.6 Styleguide shell: `components/routes/RouteStyleguide.tsx` + `components/site/styleguide/` (page header, in-page table of contents, section primitive with anchored h2, tone-band and a11y-mode switcher note); `noindex,follow`; brand sections ported from the Nuxt styleguide (palette + category colors, type, wordmark, pills, chips, cards, tone bands, photo slot, radius + shadows)
- [x] 4.7 Styleguide site components: a section per ported `components/site/**` component rendered from the fixture data (`test/fixtures` / theme fixtures), in each tone band where the component has band variants
- [x] 4.8 Styleguide kitchen sink: install every shadcn/ui registry `ui` component and its `*-example` into `components/ui` / `components/styleguide/examples/` (imports rewritten), group them like the Nuxt styleguide (form controls → overlays & navigation → data & complex), one anchored section per component; components that need providers (sonner, sidebar, form, chart) get them locally in the section
- [x] 4.9 Styleguide verification: component test that every installed `components/ui/*` has a section; e2e screenshot per section to `test-results/styleguide/`; `test:a11y` matrix for `/styleguide/` × modes green (best-practice as warnings); front-page budget unchanged (kitchen sink route-isolated)
- [ ] 4.10 Kitchen-sink a11y burn-down: the vendored shadcn registry examples carry upstream a11y debt (unnamed icon buttons, unlabeled inputs/selects, tabs `aria-controls` to unmounted panels, cmdk list children, low-contrast demo palettes); `test/e2e/a11y/kitchen-sink-baseline.json` ratchets the offending node count down as examples are patched (icon buttons get `aria-label`, inputs/selects get `<Label>`/`aria-label`, demo colors meet 4.5:1) — target 0; each patch documented at the top of the example file so upstream re-syncs stay diff-able

## 5. Chrome

- [x] 5.1 Tests first (RTL + user-event + jest-axe): `SiteHeader` (mobile nav open/close, Escape, focus returns to trigger, `aria-expanded`), `LanguageToggle` (links = current route's translations, re-homed), `SiteFooter` (columns, socials only when configured, a11y lead/link), `SkipLink`, `A11yWidget` (popover, `aria-pressed` sizes, `role=status` announcements); implement the components from `/site` props only (no hardcoded copy)
- [x] 5.2 Layout integration: `/site?lang=` per language, skip link first in DOM, `<main id="main" tabIndex={-1}>`, `components/nav/FocusManager.tsx` (focus `<main>` after route change, hash → target), verify Next's route announcer reads the new title, mount `RouteTransition`; a11y bootstrap script with nonce in `<head>`
- [x] 5.3 e2e `test/e2e/chrome.spec.ts`: chrome strings in `en`/`es`, skip link → main, mobile nav keyboard path, widget persistence (returning visitor with `xl` → root font-size 20 px at first paint), legacy key migration, focus on `<main>` after navigation, view transition present / absent under `prefers-reduced-motion`; a11y project matrix gains chrome states (mobile nav open, widget open)

## 6. Routes (per route: component tests → route component → e2e → axe)

- [x] 6.1 Front (`RouteFront`): hero (identity headline text or artwork, hero photo), who-we-are, upcoming events + `home_events_empty_*`, blog teasers + `home_blog_empty_*`, closing CTA; tests from `front-page.json`; metadata from `seo`
- [x] 6.2 Interior pages (`RoutePage`, `RouteAbout`, `RouteGetInvolved`): interior header + breadcrumb, mission band, About/Get Involved groups, committee cards, FAQ accordion, documents, grievance callout, subscribe strip; `components/site/Prose.tsx` renders kses HTML via `dangerouslySetInnerHTML` with a heading-order test and no inline handlers; tests from `page-about.json`, `page-get-involved.json`
- [x] 6.3 Posts index browse (`RoutePostsIndex`, `BlogArchive` server part, `FeaturedPostCard`, `PostCard`): featured card, grid, pagination `/blog/page/N/`, category archive `/category/{slug}/`, canonical rules; tests from `posts-envelope.json`; e2e for paged + category paths
- [x] 6.4 Posts index interactive (`BlogArchiveControls` client + `Suspense` results fragment): `?s=`/`?category=`/`?paged=` via `router.replace({ scroll: false })` debounced 300 ms, `useTransition` pending with `aria-busy` + `role=status` count, superseded queries discarded, browse mode makes no request; e2e: search is URL state and reload-stable, noscript SSR of `?s=` results, `noindex,follow`
- [x] 6.5 Single post (`RoutePost`, `SinglePost`, `PostBlocks` + `BlockProse|Image|Gallery|PullQuote|PersonQuote|Video|Audio|Document|ActionCallout|EventEmbed`): hero, article, meta rail (`showMetaRail`), sidebar, share (copy link / email), read next; a test per block type from `single-post.json` + `blog-post.json`; `next/image` for slot images, WP `srcset` kept in prose
- [x] 6.6 Calendar (`RouteCalendar`, `EventCalendar`, `MonthGrid`, `EventListView`, `EventCard`, `EventDetailDialog`): server-rendered events window as props, month nav + `aria-live` heading, `?view=list` toggle with `aria-pressed`, arrow-key grid, dialog focus trap/restore/Escape, out-of-window months via `/api/events` with `role=status` loading, `cal_empty_*` state, subscribe strip (ICS link-out absolute to WP, Google link); tests from `page-calendar.json` + `chapter-event.json`
- [x] 6.7 Single event (`RouteEvent`, `SingleEvent`, `EventBlocks`, contact, related "more upcoming"): metadata; tests from `single-event.json`
- [x] 6.8 404 and error: `not-found.tsx` twin of `404.twig` using `nf_*` strings; `error.tsx` surface with retry; e2e: unknown path → 404 status + copy in both languages; `/__mock/fail` → 500 surface on a cold route, structured log line emitted
- [x] 6.9 Parity pass: side-by-side screenshots of every route kind in `en`/`es` (Nuxt `generate:mock` preview vs Next) as review artifacts; fix gaps; promote `best-practice` axe rules to errors; full `test:a11y` matrix (route × lang × 4 modes × interactive states) green

## 7. SEO, sitemap, and the PHP canonical origin

- [x] 7.1 Test first `test/unit/metadata.spec.ts`; implement `lib/metadata.ts` (`generateMetadata`: title, description, robots, canonical verbatim, `alternates.languages` from `hreflang`, OG `url` = canonical, OG image ladder route image → `identity.share_image`; styleguide forced `noindex,follow`)
- [x] 7.2 `app/sitemap.ts` + `app/robots.ts` from `/routes` on `NEXT_PUBLIC_SITE_ORIGIN` (both languages, hreflang alternates, exclude search + styleguide); unit tests; e2e fetches both
- [x] 7.3 JSON-LD parity with spec `structured-data`: Organization (identity), Article (posts), Event (events) from envelopes via `components/seo/JsonLd.tsx`; unit tests validate required properties
- [ ] 7.4 PHP test first `tests/test-seo.php` (default no-op; `CHAPTER_CANONICAL_ORIGIN` swaps origin of canonical, every `hreflang`, `og:url`; core sitemap `<loc>` rewritten); implement `progressnow_seo_public_url()` in `inc/seo.php` (constant or `progressnow/seo/canonical_origin` filter) applied in `progressnow_seo_canonical()`, `progressnow_seo_hreflang()`, `og:url`, and `wp_sitemaps_posts_entry|wp_sitemaps_taxonomies_entry|wp_sitemaps_users_entry`; regenerate fixtures if any canonical changed; PHPUnit green
- [ ] 7.5 Nuxt (`nuxt-js`) verification — no code change expected: `npm test` green against the updated fixtures (`contracts.spec.ts`), add an explicit assertion in `test/unit/shell.spec.ts` (or a new `seo.spec.ts`) that `headForSeo` emits a foreign-origin canonical and hreflang verbatim; `generate:mock` + `verify:output` green
- [ ] 7.6 e2e `test/e2e/seo.spec.ts`: canonical/hreflang/OG/JSON-LD in both languages with the mock serving a foreign canonical origin; sitemap and robots contents

## 8. Deployment, hardening, documentation

- [ ] 8.1 Security headers + CSP (decided: nonce per request, HTML rendered per request from the cached data layer, report-only first, TTFB measured): request proxy generating a per-request nonce, `Content-Security-Policy` (`script-src 'self' 'nonce-…' 'strict-dynamic'`, `img-src 'self' data: <IMAGE_HOSTS>`, `font-src 'self'`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`), HSTS, `nosniff`, `Referrer-Policy`, `Permissions-Policy`; nonce into layout and the a11y bootstrap; e2e: headers present, zero CSP console violations on every route kind, injected nonce-less inline script blocked
- [ ] 8.2 Images: `remotePatterns` from `IMAGE_HOSTS` (default WP origin), AVIF/WebP, `dangerouslyAllowSVG: false`; e2e: unlisted host → 400 from the optimizer; card image `src` is the optimizer URL with the envelope `alt`
- [ ] 8.3 Container: multi-stage `Dockerfile` (standalone, non-root, `PORT`/`HOSTNAME`, `HEALTHCHECK /api/health`), `.dockerignore`, `scripts/smoke.mjs` (health + `/` + `/es/`); CI builds the image and runs the smoke against the mock (no push)
- [ ] 8.4 Docs: `docs/deployment.md` "Headless Next.js" (Vercel, Docker, VPS + reverse proxy; WordPress constants `CHAPTER_REBUILD_TRANSPORT=webhook`, `CHAPTER_REBUILD_WEBHOOK_URL=<origin>/api/rebuild`, `CHAPTER_REBUILD_SECRET`, `CHAPTER_CANONICAL_ORIGIN`; multi-instance cache seam; smoke test); `docs/accessibility-statement.md` (Next frontend, axe-core method, exceptions); `next-js/README.md` (commands, layout, data flow, env, tests); finalize root README table
- [ ] 8.5 Local end-to-end against MAMP WordPress (Progress Now theme active): `WP_API_BASE` real, `CHAPTER_REBUILD_TRANSPORT=webhook` → local Next, `CHAPTER_CANONICAL_ORIGIN` = Next origin; content save → page refreshes within the debounce; "Rebuild now" is immediate; admin "Site build" panel shows the build live; PHP theme pages canonical to the Next origin; both languages
- [ ] 8.6 Final gate: `next-js` CI job green (lint, typecheck, unit, build + budget, e2e, a11y matrix with best-practice as errors), `nuxt-js` job green, PHPUnit green; functional-parity checklist executed in EN and ES against the Nuxt rendition; open the PR
